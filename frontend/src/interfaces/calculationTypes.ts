/** Row shape for mytable / API calculations – extend when API is connected */
export interface CalculationRow {
  id: string
  name: string
  inGameSens: number
  dpi: number
  createdAt: string
}
